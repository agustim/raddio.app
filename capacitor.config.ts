import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.raddio',
  appName: 'lofi.moai',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
